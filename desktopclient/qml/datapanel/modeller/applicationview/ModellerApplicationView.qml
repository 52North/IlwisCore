import QtQuick 2.0
import ".." as Model

Model.ModellerWorkArea {
    property var currentApplication

    width : parent.width
    height : parent.height - 170
    color : "#F0FFFF"
    state : "invisible"
}

