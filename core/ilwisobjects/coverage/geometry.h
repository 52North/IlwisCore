#ifndef GEOMETRY_H
#define GEOMETRY_H

namespace Ilwis {
class KERNELSHARED_EXPORT Geometry
{
public:
    Geometry();

    //static geos::geom::LineString* createLine(const UPGeomFactory& factory, geos::geom::CoordinateArraySequence* coords);
    //static geos::geom::Point * createPoint(const UPGeomFactory& factory, geos::geom::Coordinate& crd);
    //static geos::geom::Polygon *createPolygon(const UPGeomFactory& factory, geos::geom::CoordinateArraySequence* outer, std::vector<geos::geom::Geometry*> *inners);


};

}

#endif // GEOMETRY_H
